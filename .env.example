# MÜŞAHİT · environment secrets
# Copy this file to .env and fill in the values.
# DO NOT commit .env to version control.

# Reddit credentials (required for social ingestion)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=MUSAHIT/0.1 (personal OSINT; your@email.com)

# SMTP for failure alerts (optional — toast notification is the fallback)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
OPERATOR_EMAIL=
